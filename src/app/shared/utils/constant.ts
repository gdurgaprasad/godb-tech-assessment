export enum PAGES {
  PUBLIC = "/public",
  LOGIN = "/public/login",
  REGISTER = "/public/register",
  USER = "/user",
}

export enum GENDERS {
  MALE = "MALE",
  FEMALE = "FEMALE",
}
export enum SNACKBAR_MESSAGES {
  REGISTRATION_SUCCESS = "User registered successfully.",
  USERS_NOT_AVAILABE = "No saved users found",
  LOGIN_SUCCESS = "Logged in successfully.",
  INVALID_CREDENTIALS_ENTERED = "Please check your credentials",
  LOG_OUT_SUCCESS = "Logged out successfully.",
}
